/** Local authentication guard. */
export const LOCAL_AUTH_GUARD = 'LOCAL_AUTH_GUARD';

/** Local only authentication guard. */
export const LOCAL_ONLY_AUTH_GUARD = 'LOCAL_ONLY_AUTH_GUARD';

/** JWT authentication guard. */
export const JWT_AUTH_GUARD = 'JWT_AUTH_GUARD';

/** Public guard. */
export const PUBLIC_GUARD = 'PUBLIC_GUARD';
