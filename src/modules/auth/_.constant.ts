/** Local authentication guard. */
export const LOCAL_AUTH_GUARD = 'LOCAL_AUTH_GUARD';

/** AccessToken authentication guard. */
export const ACCESS_TOKEN_GUARD = 'ACCESS_TOKEN_GUARD';

/** AccessToken authentication with refresh token guard. */
export const REFRESH_TOKEN_GUARD = 'REFRESH_TOKEN_GUARD';

/** Public guard. */
export const PUBLIC_GUARD = 'PUBLIC_GUARD';
